apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: test
spec:
  steps:
  - name: create configmap
    operations:
    - apply:
      - file: configmap.yaml
  - name: assert configmap
    operations:
    - assert:
      - file: configmap.yaml
